# GitHub Yayınlama

```bash
git init
git add .
git commit -m "Initial free Turkish Towny setup"
git branch -M main
git remote add origin https://github.com/KULLANICI/aykasaba-towny-setup.git
git push -u origin main
```

Release içine eklenecekler:
- `server/` config paketi
- `resourcepack.zip`
- `custom-plugin/target/moon-towny-core-1.0.0.jar`
- Kurulum dokümanları
